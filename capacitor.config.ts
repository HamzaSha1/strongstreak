import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zodwallet.strongstreak',
  appName: 'StrongStreak',
  webDir: 'dist'
};

export default config;
